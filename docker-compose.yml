services:
  afvalapp:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - db_data:/data
    environment:
      - DATABASE_URL=file:/data/afvalapp.db
    restart: unless-stopped

volumes:
  db_data:
